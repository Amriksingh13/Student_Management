@page
@model Student_Management.Pages.Results.CreateModel

@{
    ViewData["Title"] = "Create";
}
<header class="bg-dark text-white">
    <div class="container text-center">
        <h1>Welcome to Student Management System!</h1>
        <p class="lead">Manage Students and Courses.  </p>
    </div>
</header>

<h2>Create</h2>

<h4>Result</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Result.CourseId" class="control-label"></label>
                <select asp-for="Result.CourseId" class="form-control" asp-items="ViewBag.CourseId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Result.StudentId" class="control-label"></label>
                <select asp-for="Result.StudentId" class="form-control" asp-items="ViewBag.StudentId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Result.Grade" class="control-label"></label>
                <select asp-for="Result.Grade" class="form-control" asp-items="ViewBag.Grade"></select>
                <span asp-validation-for="Result.Grade" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-primary" />
            </div>
        </form>
    </div>
</div>

<div>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
